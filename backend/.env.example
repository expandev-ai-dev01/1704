# Server Configuration
NODE_ENV=development
PORT=3000
API_VERSION=v1

# Database Configuration (SQL Server)
DB_SERVER=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=your_strong_password
DB_NAME=safelogin_db
DB_ENCRYPT=true

# CORS Configuration (Frontend URLs)
# For multiple origins in production, separate them with a comma: https://app.com,https://www.app.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
