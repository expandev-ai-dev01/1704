-- Seed data for the [subscription] schema.
-- This file can be used to create a default account for development or initial setup.

-- Example:
/*
IF NOT EXISTS (SELECT 1 FROM [subscription].[account] WHERE [name] = 'Default Account')
BEGIN
    INSERT INTO [subscription].[account] ([name]) VALUES ('Default Account');
END
*/
